name: My GitHub Actions Workflow
on: [push]

jobs:
  testing_github:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello, GitHub Actions"
      - name: Display Repo name
        run: echo "This workflow is running on ${{ github.repository }}"
